server:
  port: 9999

spring:
  application:
    name: eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://simonton:9999/eureka
  instance:
    hostname: simonton